services:
  - type: web
    name: bkr-fastfood-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: bkr-fastfood-db
          property: connectionString

databases:
  - name: bkr-fastfood-db
    databaseName: fastfood_db
    user: fastfood_user